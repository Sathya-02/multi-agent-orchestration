# ════════════════════════════════════════════════════════════════
# requirements.txt — Backend Python dependencies
# ════════════════════════════════════════════════════════════════
# Install:  pip install -r requirements.txt
#
# Optional packages (install separately as needed):
#   pip install duckduckgo-search      # real web search
#   pip install yfinance               # live stock prices
#   pip install "python-telegram-bot==20.7"  # Telegram bot
#   pip install stripe                 # Stripe billing
# ════════════════════════════════════════════════════════════════

# ── CrewAI framework ──────────────────────────────────────────────────────
crewai==0.51.0
langchain-ollama==0.1.3
langchain-community==0.2.16
langchain==0.2.16

# ── FastAPI web server ────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9
websockets==12.0

# ── Data validation ───────────────────────────────────────────────────────
pydantic==2.7.4

# ── System monitoring ─────────────────────────────────────────────────────
psutil==6.0.0
requests==2.32.3

# ── File format support ───────────────────────────────────────────────────
pypdf==4.3.1
python-docx==1.1.2
openpyxl==3.1.5

# ── Utilities ─────────────────────────────────────────────────────────────
python-dateutil==2.9.0

# ── Authentication / OAuth ────────────────────────────────────────────────
# Required for Google OAuth login, session JWT cookies,
# AND the new local JWT login + RBAC system (auth.py / routers/auth_router.py)
PyJWT>=2.8.0
psycopg2-binary>=2.9.9

# ── Local auth dependencies (added with login + RBAC feature) ─────────────
# python-jose is an alternative JWT lib — PyJWT (above) is used instead.
# hashlib, secrets, json are stdlib — no extra install needed.
# pathlib is stdlib — no extra install needed.
