"""
agents/ — Agent subsystem public API
=====================================
Import from here, not from internal modules:

    from agents.definitions import BUILTIN_AGENTS
    from agents.registry    import get_active_agents, add_agent
    from agents.crew        import build_agents
    from agents.tasks       import build_tasks
"""
